import { SubstringPipePipe } from './substring-pipe.pipe';

describe('SubstringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SubstringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
